// config.js — point this at your deployed server.
// For local dev with a physical phone, use your computer's LAN IP, e.g.
// "http://192.168.1.23:4000" (localhost won't work from a real device).
export const SERVER_URL = "http://localhost:4000";
